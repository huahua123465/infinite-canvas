---
title: 官方 v0.8 合并记录
description: 记录官方 v0.8 与当前定制项目的分阶段合并范围、插件分发方式、验证项和后续计划。
---

# 官方 v0.8 合并记录

本文是官方 v0.8 合并工作的长期维护入口。后续继续合并时先阅读本文，再核对[待测试](/docs/progress/pending-test)和当前工作区，避免依赖聊天记录重复判断。

## 当前结论

- 第一阶段已经完成并由用户确认测试通过：官方 v0.8 的画布节点插件宿主核心已增量接入当前项目。
- 第二阶段代码已经完成，等待人工回归：SDK、模板、五个示例插件源码、浏览器 ESM、静态清单和固定依赖均已进入仓库。
- 当前实现不会覆盖原有画布主文件，也没有把 Script、Workspace、Group 或生成链路迁入官方统一渲染器。
- 插件安装记录、缓存源码、启用状态和插件私有数据保存在当前浏览器的 IndexedDB/localforage，不会随 Git 提交同步给其他电脑或浏览器。
- 同事拉取代码后无需联网下载内置插件；管理器只读取仓库静态清单，第一次仍需在“内置可选”中点击“安装”，此时才执行对应插件代码。

## 同事拉取后的实际行为

| 内容 | 是否随 Git 拉取 | 说明 |
| --- | --- | --- |
| 插件管理弹窗与安全提示 | 是 | 拉取并启动前端后即可使用 |
| 动态节点注册、渲染、面板、工具栏和错误隔离 | 是 | 属于画布宿主运行时代码 |
| SDK、模板与五个示例插件源码 | 是 | 位于 `plugins/canvas/` |
| 五个内置浏览器 ESM 与固定依赖 | 是 | 位于 `web/public/plugins/`，未安装前不执行 |
| 内置插件静态清单 | 是 | 只读取 JSON 元信息，不导入插件模块 |
| 官方插件列表 | 否，运行时远程读取 | 默认从官方 `plugins-dist` 注册表获取 |
| 已安装/启用状态 | 否 | 每个浏览器独立保存 |
| 插件节点在画布中的 metadata | 随画布项目数据保存 | 但打开该节点的浏览器仍需安装并启用对应插件 |

同事第一次使用步骤：

1. 拉取 `custom/jt-vite-merge` 分支并按原方式启动 Web。
2. 打开任意画布，点击右上角“节点插件”。
3. 在“内置可选”中安装所需插件；该过程读取仓库静态文件，不依赖官方 CDN。
4. 关闭弹窗，通过双击画布空白处、连线创建菜单或底部工具栏创建插件节点。
5. 如果画布已有插件节点但当前浏览器未安装插件，节点会显示“缺少插件”；安装并启用同 ID 插件后恢复渲染，节点数据不会被删除。

## 当前仓库内置方式

- `web/public/plugins/catalog.json` 只保存插件名称、版本、说明、图标和相对 ESM 地址。
- 打开管理器只读取清单，不通过动态 import 执行插件。
- 用户点击“安装”后，宿主才从当前站点 `/plugins/` 目录直接加载 ESM，并将记录写入 localforage。
- 内置直载通道限制为当前站点 `/plugins/`；远程官方和第三方插件继续走源码下载、Blob 隔离和缓存路径。
- 内置插件未安装、未启用时不注册节点，不影响现有项目启动；安装后可继续禁用或卸载。

## 第一阶段已完成范围

### 插件管理与分发

- 支持官方插件注册表，并兼容清单中的相对 `entry` 和绝对 `url`。
- 支持第三方 HTTP/HTTPS 插件 URL 安装。
- 支持 `/plugins/index.json` 本地插件发现和 `VITE_DEV_PLUGINS` 开发插件加载。
- 支持安装、启用、禁用、来源更新和卸载。
- 更新时要求插件 ID 保持不变；新版本激活失败时尝试恢复旧版本。
- 管理器明确提示插件代码在当前页面执行，可以访问浏览器本地数据和 AI API Key，只应安装可信来源。

### 节点能力

- 插件节点类型使用独立动态注册表，不能覆盖内置节点，也不能与其他插件类型冲突。
- 双击创建菜单、连线创建菜单和底部工具栏可创建插件节点。
- 支持插件节点 Content、Panel、悬浮工具栏、双击处理、尺寸比例规则和连接点规则。
- 支持插件自定义小地图颜色。
- 插件节点可作为上游文本、图片、视频或音频资源。
- Canvas Agent 底层操作可以创建当前已注册的插件节点。

### 稳定性与数据

- 插件节点内容和面板使用错误边界，单个插件渲染失败不应让整张画布白屏。
- 插件工具栏、资源读取、双击行为、比例规则和清理函数分别隔离异常。
- 插件源码和安装状态使用 localforage 保存；插件私有存储按插件 ID 隔离。
- 禁用或卸载插件只移除运行时定义和样式，不删除已有画布节点。
- 缺少插件时显示占位提示；重新安装同 ID 插件后恢复渲染。
- 插件节点信息弹窗显示插件标题，未知或禁用类型显示原始类型 ID。

### 插件交互补丁

- 便利贴等带编辑按钮的插件支持双击正文进入编辑。
- 插件正文的非交互区域可以拖动节点，支持上下左右移动。
- 按钮、输入框、链接、iframe、canvas、可编辑区域和 `data-canvas-no-drag` 区域不会误触发节点拖动。
- 双击画布打开“选择节点”菜单后，点击菜单外任意区域即可关闭，右上角关闭按钮继续保留。

## 第二阶段已完成范围

- TypeScript SDK：公开类型、`definePlugin`、automatic JSX、宿主 React hooks 桥接和统一 esbuild 构建助手。
- 插件模板：独立 `package.json`、TypeScript 配置、构建入口、示例节点和开发说明。
- 便利贴：换色、编辑、衍生文本、双击正文和非交互区域拖动。
- HTML：无同源权限 sandbox iframe、CSP、上游 `{{input}}` 转义和内联脚本边界。
- Markdown：固定 `marked 17.0.6`，渲染前使用 `DOMPurify 3.4.12` HTML profile 净化。
- SVG：DOMPurify SVG profile，禁止脚本、`foreignObject`、`href` 和 `xlink:href`。
- 3D 全景：固定 `Three.js 0.180.0`，不依赖运行时 CDN，并清理动画、监听器、纹理和 WebGL 上下文。
- 仓库内置分发：静态清单、五个浏览器 ESM、固定依赖和许可证；默认未安装、未启用。

第二阶段独立提交：

- `a411a15 feat(plugins): add TypeScript SDK and template`
- `b7ddb9a feat(plugins): add sticky note and sandboxed HTML`
- `5a09ae2 feat(plugins): add sanitized Markdown and SVG`
- `1c4d70b feat(plugins): add bundled panorama viewer`
- `ec5aa3e feat(plugins): add optional bundled catalog`

## 明确保留的定制能力

前两个阶段均采用插件分流和独立静态资源接入，不替换下列能力：

- Script 三步人工审核、事实镜头与动态生产镜头区分、分镜资产和最终视频提示词。
- Workspace 与 Group 的独立语义和画布行为。
- 沧元图片/视频、Seedance、Ark、Gemini、火山音频、VoxCPM 和 BS-Roformer。
- 本地 Canvas Agent、站点级 Agent、媒体代理和浏览器本地持久化。
- 当前图片、视频、音频、文本、配置和工作区节点的原有渲染与生成链路。

上述能力在代码结构上已保留，但仍需按待测试清单进行人工回归，不能仅凭静态合并认定全部通过。

## 当前仍未接入

- 官方插件构建脚本及自动同步到 `web/public/plugins` 的开发流程。
- 官方模型自定义调用脚本或与当前 Provider 路由冲突的扩展。
- 用官方画布主文件整体替换当前 `project.tsx`、`canvas-node.tsx` 或节点类型定义。
- 插件安全沙箱、权限声明、metadata 数据迁移和跨插件依赖协议。

## 人工验证清单

### 安装与恢复

- “内置可选”列表能显示 Markdown、SVG、HTML、3D 全景和便利贴。
- 安装后节点类型立即出现在创建入口，无需刷新或重新安装。
- 禁用后已有节点显示“缺少插件”，重新启用后恢复内容。
- 更新失败时旧版本仍能恢复；卸载不会删除画布节点数据。

### 节点交互

- 便利贴正文双击后出现输入框，可以输入并保存文字。
- 从便利贴正文空白区域拖动时，节点能向任意方向移动。
- 点击换色、衍生文本、编辑按钮或输入框时不会移动节点。
- Markdown 可编辑和预览；SVG 可编辑源码或读取上游 SVG 文本。
- HTML 可在沙箱 iframe 中预览，并用 `{{input}}` 注入上游文本。
- 3D 全景连接等距柱状全景图片后可以拖拽旋转，不会移动节点本身。

### 原功能回归

- 创建和编辑内置文本、图片、视频、音频、配置、Script、Group 和 Workspace 节点。
- 打开 Script 三步工作流并执行一次资产、提示词或视频入口。
- 验证本地 Agent 和现有 Provider 配置仍可读取当前画布。
- 验证刷新、导入、导出和已有项目恢复不受插件节点类型放宽影响。

## 关键代码入口

- `web/src/pages/canvas/project.tsx`：插件加载、宿主上下文、创建菜单、面板和工具栏接线。
- `web/src/components/canvas/canvas-node.tsx`：插件内容渲染、错误隔离、双击与拖动边界。
- `web/src/components/canvas/canvas-plugin-manager-modal.tsx`：插件安装与管理界面。
- `web/src/lib/canvas/plugin-loader.ts`：插件下载、校验、激活、更新和恢复。
- `web/src/lib/canvas/node-registry.ts`：动态节点注册表和类型冲突保护。
- `web/src/stores/canvas/use-plugin-store.ts`：浏览器本地插件记录。
- `web/src/types/canvas-plugin.ts`：插件宿主契约。
- `plugins/canvas/`：SDK、模板和五个示例插件 TypeScript 源码。
- `web/public/plugins/catalog.json`：内置可选插件静态清单。
- `web/public/plugins/`：浏览器 ESM、固定依赖和第三方许可证。

## 推荐合并顺序

以下四个阶段是后续工作的固定基线，不再根据聊天记录临时改序。

### 第一阶段：节点插件核心

状态：**已完成，用户测试通过**。

已合并：

- 节点注册表。
- 插件运行时。
- 插件加载器。
- 插件 localforage 存储。
- 官方插件注册表。
- 右上角“节点插件”入口。
- 插件缺失占位和错误隔离。
- 插件节点创建、资源、面板、工具栏、小地图、双击编辑和正文拖动接线。
- 保留现有全部内置节点、Script、Workspace、Group 和分镜节点。

关键提交：`72c7948 feat(canvas): integrate v0.8 node plugins`。

### 第二阶段：SDK 与示例插件

状态：**已完成，等待人工回归**。

已合并：

- TypeScript SDK。
- 插件模板。
- 便利贴。
- HTML。
- Markdown、SVG、3D 全景在安全处理后接入。
- 插件构建和仓库内分发方式。

约束已落实：所有示例插件默认不安装、不启用；静态清单加载不会执行插件代码，用户点击安装后才加载仓库内 ESM。

### 第三阶段：自定义模型调用脚本

状态：**未开始**。

计划单独接入：

- 模型脚本编辑器。
- OpenAI/Gemini 模板。
- 图片、视频、音频、文本脚本执行入口。
- 空脚本继续走当前原生渠道。

约束：不替换当前 Provider 配置、模型选择器和请求服务；按媒体类型独立接线，避免脚本异常影响现有生成链路。

### 第四阶段：文档和版本收口

状态：**未开始正式收口**。

完成第二、三阶段并由用户人工测试后再执行：

- 整理 `pending-test.mdx`。
- 整理 `todo.mdx`。
- 将 `CHANGELOG.md` 的 `Unreleased` 归档为正式版本。
- 更新根目录 `VERSION`。
- 创建版本提交和 tag。

当前 `pending-test.mdx`、`todo.mdx`、本页和 `CHANGELOG.md` 的 `Unreleased` 只用于记录开发过程，这是项目约束要求的过程文档更新；根目录 `VERSION` 尚未修改，也没有执行第四阶段发版。

## 后续推进规则

1. 每个阶段先在本文写清楚同步范围、明确不做项和回归风险。
2. 继续采用增量分流，不整文件覆盖画布、生成 API 或 Agent 服务。
3. 代码完成后先更新[待测试](/docs/progress/pending-test)，用户确认后再更新正式功能说明。
4. 每次提交在本文补充阶段状态和关键提交，避免依赖聊天记录。
5. 第二阶段人工回归通过后再开始第三阶段，不把 SDK、示例插件和自定义模型脚本混在同一个提交中。

## 阶段状态

| 阶段 | 状态 | 内容 |
| --- | --- | --- |
| 第一阶段：节点插件核心 | 已完成，测试通过 | 注册表、运行时、加载器、localforage、管理器、动态节点接线和故障隔离 |
| 第二阶段：SDK 与示例插件 | 已完成，待人工回归 | TypeScript SDK、模板、安全示例插件和仓库内置静态分发；默认不安装、不启用 |
| 第三阶段：自定义模型调用脚本 | 未开始 | 脚本编辑器、模板和各媒体执行入口；空脚本保持原生渠道 |
| 第四阶段：文档和版本收口 | 未开始正式收口 | 用户测试后整理进度文档、CHANGELOG、VERSION、提交和 tag |

## 相关记录

- [待测试](/docs/progress/pending-test)
- [TODO](/docs/progress/todo)
- [custom/jt-vite-merge 融合记录](/docs/progress/jt-vite-merge-notes)
- `CHANGELOG.md` 的 `Unreleased`
