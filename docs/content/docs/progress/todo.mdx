---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- 本地 Agent 接入后续：按[本地 Agent 接入规划](/docs/progress/local-agent-integration-plan)把 Claude Code CLI Adapter 升级为 Claude Agent SDK Adapter，并完善工具队列体验。
- 官方 v0.9 同步回归：已按功能增量同步官方 `v0.9.0` 与当前 `main`，等待人工验证侧栏/导出、提示词来源脚本、插件 1.1/1.2、透明背景、缩略图引用和多标签页 Agent；同时继续完成原 v0.8 自定义模型脚本、3D 全景 Three.js 模块和浅色/深色主题回归。全部通过后再整理正式版本记录并创建发布 tag。
- 视频任务误提交保护：沧元公开 API 暂无暂停或取消任务端点；视频任务正式提交前增加短暂可撤回阶段，拿到任务 ID 后把“停止生成”明确区分为“停止等待”，并在同镜头再次创建版本前提示旧任务仍可能继续运行和计费。
- 漫剧助手第二阶段：按[漫剧助手第二阶段实施计划](/docs/progress/manga-assistant-phase2-plan)将现有 Script 节点产品化为单节点漫剧工作台，依次完成事实审核、场景卡、资产 ID 绑定、风格继承、单镜头提示词、视频版本和逐项恢复；每阶段完成后移入待测试清单。
- 沧元视频单模型实测收口：用最小真实请求确认 `seedance-2.0-mini-8s` 的请求模型别名、SD5 参考视频时长限制和 Grok 旧端点是否仍可用；确认后再接入 SD5 `seed`、`negative_prompt` 与互斥的成对首尾帧模式，避免在缺少独立数据入口时把普通参考图误发为帧模式。
