# AGENTS.md

本文档用于约束本项目中的 AI / 自动化开发行为。开发时优先遵循本文件，其次遵循用户当前消息。

## 基本原则

- 先读现有代码，再动手修改，优先沿用项目已有结构和写法。
- 写代码保持最少行数，能简单实现就不要引入复杂抽象。
- 标准格式、协议、解析、压缩、加密、日期等通用能力优先使用成熟稳定的库，不要手写底层实现，除非用户明确要求或项目已有实现必须沿用。
- 不要为了“兼容更多场景”写大量分支，只实现当前明确需要的功能。
- 项目尚未上线，不需要兼容旧数据；表结构或字段调整时直接按新设计修改，不写旧字段兼容、数据迁移兜底或删除旧表的清理逻辑，除非用户明确要求。
- 每次写完代码，不需要检查语法，不需要执行构建，用户会自己做。
- 不要改无关文件，不要顺手重构。
- 如果工作区已有用户改动，不要回滚，不要覆盖；只在必要范围内追加修改。

## 反复提醒沉淀

- 如果开发过程中总是遇到某个问题，或者用户反复提醒同一个注意事项，需要把该注意事项补充到本文件。
- 补充时写成明确、可执行的规则，避免只写模糊描述。
- 新规则应放到最相关的章节；找不到合适章节时放到“项目注意事项”。

## 后端规范

- 后端使用 Go + Gin + GORM。
- `handler/` 只处理 HTTP 入参、调用 service、返回 `OK` / `Fail`。
- `service/` 放业务逻辑、默认值、校验、时间、ID、鉴权等处理。
- `repository/` 只做数据库访问和 GORM 查询。
- `model/` 只定义数据结构、枚举和简单模型方法。
- 列表接口优先沿用 `model.Query`、`Normalize`、分页和标签筛选方式。
- 业务接口保持 `{ code, data, msg }` 的响应结构。
- 新增数据表时同步更新 `docs/backend-database.md`。

## 前端规范

- 前端使用 Vite、React、React Router、TypeScript、Ant Design、Tailwind、Zustand。
- 编写 Ant Design 相关代码时，参考 https://ant.design/llms-full.txt 理解组件 API、示例和设计规范，并优先结合项目当前 antd 版本与既有写法。
- API 请求统一放在 `web/src/services/api/`。
- 全局或跨页面状态优先放在 `web/src/stores/`。
- 已经放在全局 store 或全局 hook 中的状态/动作，组件需要时直接使用对应 store/hook，不要为了“纯组件”层层透传 props；避免一个组件传递过多参数。
- 全局组件、全局常量、全局配置等全局性质的内容不要作为 props 或参数层层传递；哪里需要就在哪里直接从对应全局入口获取。
- 多个页面重复出现的 UI 副作用动作，例如复制文本并提示、下载并提示、统一确认弹窗，优先抽成 `web/src/hooks/` 下的全局 hook；不要放进 store，除非它确实是需要共享/订阅的状态。
- 路由页面放在 `web/src/pages/`，页面布局放在 `web/src/layouts/`，路由配置放在 `web/src/router.tsx`。
- 画布页面放在 `web/src/pages/canvas/`，画布组件放在 `web/src/components/canvas/`，画布状态放在 `web/src/stores/canvas/`，画布工具函数放在 `web/src/lib/canvas/`。
- 页面按目录组织，例如 `web/src/pages/image/index.tsx`；页面里只有一个主业务组件时直接写在对应页面入口中，不要单独拆 `Manager` 组件再传一堆 props。
- 不要新增只做简单转发的组件，例如只 `return <X>{children}</X>` 或只换个名字透传 props；直接在使用处使用真实组件或把逻辑写进当前文件。
- 页面私有 hook 放在对应页面目录下，例如 `admin/assets/use-admin-assets.ts`；只有多个页面真实复用的 hook 才放到外层 `hooks/`。
- 管理后台页面私有组件放到各自页面目录的 `components/` 下，例如 `admin/assets/components/`、`admin/prompts/components/`；不要为了单页面使用放到 `admin/components/` 共享目录。
- 管理后台主题、背景、卡片阴影、表格配色等统一在 `web/src/lib/app-theme.ts`、`AppProviders` 或必要的全局 CSS 作用域中配置；页面私有组件不要自己写 `dark ? ...` 主题分支。
- 组件优先使用函数组件和现有 hooks，不新增大型状态管理方案。
- UI 图标优先使用 `lucide-react` 或项目已经使用的 Ant Design 图标。
- 页面文案保持中文。
- 不要在组件里堆太多无关逻辑；复杂逻辑优先抽成同目录工具函数或小组件。
- 样式优先由组件自己管理；组件私有样式优先使用 Tailwind className 或少量内联 style，不要为单个组件新增大量全局 CSS。
- 全局 CSS 只放基础变量、全局重置、跨页面通用样式和少量第三方组件必要覆盖；不要在 `globals.css` 堆页面私有样式。
- 代码尽量短小直接，少拆不必要组件，少做多层 props 传递，避免为了抽象堆出更多代码。
- 修改组件 props 或函数签名时，必须同步清理组件体、effect 依赖和调用处中的旧参数引用，避免通过类型之外的自由变量在运行时触发 `ReferenceError`。
- 前端业务数据需要浏览器本地持久化时，默认使用 `localforage`；`localStorage` 只用于极小的简单配置，不要用来保存业务列表、生成记录、图片、base64 或大 JSON。

## 画布 UI 规范

- 做 canvas 前端 UI 时必须遵循当前画布主题。
- 优先使用 `canvasThemes`、`useThemeStore` 或 Ant Design `ConfigProvider` token。
- 不要硬编码黑白、stone、slate 等颜色导致浅色/深色主题不一致。
- 新增画布按钮、弹窗、浮层时，尽量复用已有工具栏、节点面板、Modal 的视觉风格。
- 画布顶部工具栏和状态信息优先采用极简扁平风格：无边框、无阴影、无胶囊背景，融入整体背景，弱化按钮感，仅保留轻微 hover 反馈，保持简洁现代、低视觉重量。
- 图片节点尺寸逻辑要尊重原始比例，除非功能明确要求自由变形。
- 批量生成、多图展示、助手面板等画布交互要尽量简洁，不要占用过多画布空间。

## 文档规范

- README 保持简洁，只放项目介绍、核心功能、快速开始和文档入口。
- `docs/index.md` 放给 AI 使用的文档索引，不要再放到 `docs/content/docs/` 内容目录里。
- 详细功能介绍写到 `docs/content/docs/overview/features.mdx`。
- 后续待办写到 `docs/content/docs/progress/todo.mdx`。
- 已实现但还需要用户测试确认的事项写到 `docs/content/docs/progress/pending-test.mdx`。
- `docs/content/docs/progress/pending-test.mdx` 用来记录这个版本实际做了哪些可测试变更；`CHANGELOG.md` 的 `Unreleased` 只保留对这些变更的版本级归纳，避免逐条照搬实现细节。
- 每次 todo 事项完成后，先从 `docs/content/docs/progress/todo.mdx` 移到 `docs/content/docs/progress/pending-test.mdx`，不要直接写进正式功能说明；用户确认测试通过后再更新 `docs/content/docs/overview/features.mdx`。
- 每次任务完成前，都要根据实际变更检查并更新 `docs/content/docs/progress/todo.mdx` 和 `docs/content/docs/progress/pending-test.mdx`；如果功能或待办没有变化，也要确认无需修改。
- 接口响应规则写到 `docs/content/docs/backend/api-response.mdx`。
- 数据库结构写到 `docs/content/docs/backend/backend-database.mdx`。
- 文档不要写过期日期；除非用户明确要求记录具体时间。

### 完成代码后的知识沉淀与收口

- 按实际变更检查并更新 `docs/content/docs/progress/todo.mdx` 和 `docs/content/docs/progress/pending-test.mdx`。
- 检查本次是否产生新的架构决策、模型实验结论、可复用踩坑、用户需求证据或阶段判断。
- 存在可复用结论时，使用 `project-notes/90-模板/` 中对应模板更新知识库及其索引；不要把 TODO、待测试清单、功能说明或 CHANGELOG 复制进知识库。
- 没有知识库更新时，在交付结果中明确说明原因。
- 交付结果必须说明已完成的更新、仍需验证的部分以及相对上一版本最突出的功能差异。

## 发版本流程

- 发版本时，先把 `CHANGELOG.md` 的 `Unreleased` 变更整理成新的版本记录，并保留空的 `Unreleased` 标题。
- 按当前版本号提升一个版本，更新根目录 `VERSION`。
- 将当前未提交的代码全部提交到 Git。
- 提交完成后，给当前提交打最新版本号对应的 tag，例如 `v0.0.5`。
- 发版本流程中不要执行编译、测试或构建，除非用户明确要求。

## 项目注意事项

- 涉及画布交互、节点渲染、生成链路、分镜工作流、模型渠道或本地 Agent 的复杂改动时，优先遵循 `.agents/skills/infinite-canvas-change/SKILL.md`，先分类再小范围修改。
- 沧元图生图必须同步把参考图作为 multipart `image` 文件提交到 `/v1/images/edits`，不得附带 `async=true`；输入框、配置节点转换和最终请求提示词必须全程保留 `@图片N` 绑定标记，旧提示词中的裸 `图片N` 引用应在请求前自动补全 `@`，不得只上传文件却把引用降级成普通文字。提示词明确要求修改宽高比时不得继续发送节点中的冲突旧比例；单图编辑必须明确要求基于原图并保留用户未指定修改的内容，避免生成式编辑退化成无关重绘。
- 接入沧元视频模型时必须逐个核对“单模型 API 说明”，不得把 `seedance-2.0` 标准模型与 `seedance-2.0-480p/720p/1080p/4k` 固定分辨率模型共用同一套字段和素材上限；前端只能把本地记录称为“请求模型”，不能称为平台“实际上游模型”。
- 沧元模型广场 VIDEO 分组的界面提示、生成预检和 API 提交必须共用按 public model 名维护的能力定义：标准 `seedance-2.0/fast/mini/mini-8s` 为 4 图/3 视频/1 音频，固定档位 `480p/720p/1080p/4k` 及 fast/mini 固定档位为 9 图/3 视频/3 音频，SD5 为 9/3/3 且三类合计不超过 12，Omni Frame 为 5/0/0，Omni V2V 为 0/1/0，Sora 2 为 1/0/0。不得因平台内部 `videos-4` / `videos-4-mini` 线路错误降低公开 933 契约，也不得静默 `slice` 素材；应按文档完整提交，并把嵌套 `detail` 作为上游路由不一致的证据展示。
- 沧元 `sd5-seedance-2.0` / `sd5-seedance-2.0-fast` 必须按当前单模型文档使用 `reference_mode=media + reference_image_urls/reference_videos/reference_audios` 提交全能参考；只有互斥的成对首尾帧使用 `reference_mode=frame + first_image_url + last_image_url`。不得继续把兼容别名 `images` 当作推荐主字段。
- 沧元固定分辨率 Seedance 的 `image_url` 与 `reference_image_urls` 支持公网 URL 或 `data:image/...;base64,...`；多张本地参考图必须按同序 JSON 字段提交，不得因 multipart `image` 仅支持单图而错误阻止 Base64 多图。参考视频和参考音频仍按单模型文档要求使用公网 HTTPS URL。
- 沧元固定分辨率 Seedance 的全能参考请求包含本地视频/音频或超过 4 张本地图时，必须把本地参考图与视频一起临时发布为 HTTPS，避免多张 Base64 图片导致异步转发 JSON 过大或截断；普通全能参考的全部图片必须按顺序放入 `reference_image_urls`，不得因存在参考视频或单独的首帧角色而擅自把第 1 张拆成 `image_url`。只有恰好提供成对首帧与尾帧且没有任何其他参考图、视频或音频时，才允许使用互斥的 `first_image_url + last_image_url`。
- 沧元 Seedance 使用本地参考视频时，生成链路必须自动把浏览器本地文件临时发布为公网 HTTPS URL，再写入 `reference_videos`；直链须保留原始视频扩展名语义，并在提交前确认可读取且响应类型为 `video/*`。默认临时地址须在一小时内自动过期，任务完成或明确失败后应主动提前删除。发布失败或返回非 HTTPS 地址时不得创建付费视频任务，不得要求用户手工复制命令或回填 URL，也不得把 `blob:`、`data:video` 或本地路径直接提交给沧元。
- 画布视频参考素材的界面预览、提示词标签和最终请求数组必须来自同一份仅沿入线收集的有序列表；不得用双向画布遍历组装生成请求，也不得把历史生成结果回流为新参考。提交沧元 Seedance 前必须把内部节点引用、`@图片N`、`@imageN` 和已连接素材文件名统一归一为与实际上传数组同序的 `@imageN`。
- Script 画布导出资产时，如果对应待审核视频草稿已经存在，必须同步重算视频草稿的实际参考数组并补齐“资产节点 → 视频草稿”入线；重算必须同时读取第一步画面描述、场景卡动作链、已保存的首帧提示词和视频运动提示词，旧项目不得要求用户重新合成提示词才能补齐参考。新的自动参考集合必须替换旧的自动人物/场景/道具参考，只保留首尾帧与用户手工加入的参考。普通人物镜头必须分别选择一个最匹配时期的人物参考和一个最匹配地点的场景参考，并且只在当前画面、视觉事实、动作节拍或已保存提示词确实出现/使用道具时自动选择最多一个最匹配关键道具；人物和场景严格限制当前章节，道具候选不得因错误或缺失的 `chapterIds` 被提前排除，而应从全系列道具中按当前镜头动作强匹配。道具必须完整命中资产名或至少命中资产名的中文二字片段，资产描述和提示词只能辅助排序，不能单独触发绑定。不能用“每镜已有任意一张参考图”代替场景绑定，也不得把全部道具连接到全部镜头；用户手工加入的参考优先，未进入最终参考数组的资产不得连线。视频提示词正文必须在人物、场景、道具或首帧对应栏目使用与入线素材完全一致的 `@资产名`，场景多角度图必须使用实际 `@场景名-多角度锁定图`；参考预览、画布入线、提示词引用、最终 `@imageN` 顺序和实际请求数组必须保持一致。
- 当前画布项目和“我的素材”主要保存在浏览器本地，不要在文档中误写成已支持云同步。
- 当前 AI API Key 存在浏览器本地，并由前端直接请求 OpenAI 兼容接口；涉及安全说明时要写清楚。
- Docker 静态资源路径目前仍是待办项，文档中不要过度承诺生产部署已经完全验证。
- Script 工作流必须区分“完整事实分镜数”和“实际动态生产数”：第三步批量合成、进度统计和批量视频只能按 `storyboardShotPlans.renderMode` 处理动态镜头，静态事实镜头不得计入动态任务总数；界面文案不得把完整分镜数表述成待生成视频数。
- 批量处理 AI 结构化输出时必须逐项隔离失败：单项 JSON 异常应先自动修复并有限重试，仍失败则保存该项错误并继续后续任务，不得让一个坏响应终止整个批次；再次执行时跳过已成功项并只重试失败/剩余项。
- Script 第三步“合成提示词”每个镜头最多只能发起一次付费文本模型请求；模型原始响应必须按镜头连同模型、规则、场景卡和资产上下文指纹保存。JSON 解析、格式、动作或生产契约校验失败后，同一上下文先对已保存响应做本地确定性校正，不得再调用模型做语义重试或 JSON 修复，也不得用本地生成的替代提示词冒充模型结果。校正仍失败时保存错误并继续后续镜头；主动重新合成或上下文变化时不得复用旧响应，确需发起新请求时界面必须明确提示。失败镜头即使保留了旧提示词，也必须在对应行显示错误且不得计入完成数或直接生成视频，禁止出现“11/11 完成”同时又显示“失败 3”却找不到失败行的矛盾状态。
- Script 第三步的动态 `videoMotionPrompt` 必须按当前片段总时长输出明确的四段式秒级时间轴，从 0 秒连续覆盖到结束，并固定包含生成规格、资产绑定、叙事目标、起始画面、镜头运动、光线、声音时间轴和稳定约束；0-3 秒必须在建立场景与站位的同时执行第一步动作链首拍，不得只写建场，最后 1-2 秒保留稳定落点。场景必须在起始画面或首段使用精确 `@资产名`，已绑定且可见的人物和道具必须在实际参与的时间段使用精确 `@资产名`；技能包规则或内置规则驱动的模型结果及其本地确定性校正必须使用同一格式。
- Script 第三步检查视频时间轴是否包含可执行动作时，必须优先复用 `typedActionBeats` 与第一步 `visual` 的结构化覆盖；词汇兜底只能在同一动作子句内同时命中身体部位或场景卡道具与明确物理动词，并排除运镜子句、心理描述和“保持不动/保持静止”等否定状态。不得用少量固定词表误拒“右手握木杖、左手牵人、右腿蹬地、跨过石阶”等明确动作，也不得把“身体保持不动，镜头推近”误判为人物动作。
- Script 处理传记、叙事、概念短片或系列故事时，必须在事实提取/单集浓缩完成后、片段规划前生成可追溯的“剧作总纲”，并让镜头、资产和最终视频提示词继承其戏剧功能与双轨节奏；所有判断只能引用当前事实 ID，不得为增强戏剧性虚构人物、关系、对白、道具、地点、冲突或结局，且不得跳过既有三步人工审核。
- 视频生成前的启发式连续性检查只用于提示风险：同名角色、人物时期或场景候选的模糊匹配不得直接禁用“生成视频”；最终提示词非空时应允许用户显式继续，并由统一视频预检只对缺模型参数、素材不可读取等确定性错误执行硬阻断。
- Script 单集规划必须先锁定动态视频预算，再在预算内分配事实；每个普通视频只能有一个主要可见事实、一个地点和一个人物时期，其余相邻背景事实只能由同场旁白承载。不得通过增加视频数解决过载，也不得在最后 1-2 秒引入新人物、地点、道具或剧情；旁白按每秒约 4 个汉字控制，并与画面时间段严格对齐。
- Script 分镜生产契约判断“多个故事时期”时，只能对明确岁数及婴儿、年少、少年、青年、成年、中年、晚年等可归一的人物年龄阶段做硬冲突；“抵达某地后”“相遇时”等事件内相对时点不得仅因文案不同而拦截。参与者和角色资产中真实的年龄阶段冲突仍必须拦截。
- Script 第一步的每个生产片段必须先形成可执行场景卡：明确当前目标、失败代价、人物策略、2-4 个因果动作节拍、阻力反作用、动作转折、可见结果、价值变化和起止状态；`visual` 必须固定写成 0-3、3-9、9-12、12-15 秒四段详细动作，前三段逐段写清执行者、身体部位或道具、动作对象与物理结果，禁止用“木杖落地、人物前行、动作继续”等短标签，最后一段只保持结果和稳定落点。第一步只使用语义人物/场景/道具名，不得提前虚构 `@资产名`；缺少任一核心字段或详细时间轴时必须在写入分镜前有限重写，第三步按同一动作链生成时间轴。
- Script 画布“导出资产”只允许把已经生成或上传的资产转换为画布节点，不得隐式调用图片模型；完整系列的当前集操作不得自动识别或生成尚未切换、尚未人工确认的后续章节资产。
- Script 第三步提示词修正成功后，如果当前章节已经存在视频工作区，必须在不调用文本或视频模型的前提下自动补建缺失的待审核视频草稿；新增草稿只能追加到未占用位置，不得覆盖或移动现有草稿。已有视频结果不能代替或删除对应草稿，草稿重建后必须恢复结果索引；仍未通过生产契约的镜头不得伪造草稿，并须把具体错误写回第三步红色失败状态。带有旧错误但仍保存模型提示词的缺失镜头也必须进入本地升级候选，先复用确定性校正路径恢复锁定语音、动作、资产绑定、结果和稳定落点，校正与草稿校验都通过后再原子清错、更新第 9 列并补草稿；不得因 `storyboardPromptErrors` 提前过滤而永远无法恢复，也不得在该路径发起任何 API 请求。安全中性化等全文改写必须发生在恢复锁定语音之前；锁定旁白/对白写回后，草稿构建和视频提交路径不得再次改写整段最终提示词。
- Voicebox 的 `VOICEBOX_MODELS_DIR`、`HF_HOME`、Hugging Face Hub/Xet/Assets、Torch 和 XDG 模型缓存必须统一指向项目内 `voicebox/.runtime/models/`，Transformers 复用 `HF_HOME`，不得继续设置已弃用的 `TRANSFORMERS_CACHE` 或默认写入系统 C 盘用户缓存；迁移已有全局缓存前必须确认其内容和目标绝对路径，不得由启动脚本自动搬走其他项目共享的模型。
- `voicebox/` 是自动拉取且被 Git 忽略的固定上游源码；项目级 Voicebox Web 定制必须作为 `voicebox-patches/*.patch` 提交，并由统一启动脚本幂等应用、在补丁哈希变化时重建 Web，不得只修改本机 `voicebox/` 后交付。
- 画布中的 Voicebox 输入区必须等价于 Voicebox Web 的生成输入区：选择档案后只以真实档案 ID 调用 Voicebox，并由档案决定引擎、预设/克隆音色和默认效果；不得按档案名称重新映射音色，也不得用画布音频结果缓存跳过新的 Voicebox 任务。生成节点必须保留档案 ID 与 Voicebox 任务 ID，便于核对 Web 历史和画布返回音频来自同一次生成。
